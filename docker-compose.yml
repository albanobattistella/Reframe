
services:
  reframe:
    build: .
    image: reframe:latest
    container_name: reframe
    ports:
      - "8080:8080"
    volumes:
      - ./media/uploads:/app/uploads
      - ./media/exports:/app/exports
    restart: unless-stopped
